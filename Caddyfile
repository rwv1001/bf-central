# Caddyfile for bf-central
# LAN-only deployment: serves plain HTTP on port 80.
# Let's Encrypt cannot issue certificates for private IPs.

http://{$CENTRAL_DOMAIN} {
    reverse_proxy central-api:5000
}
